<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Invoice <%= invoice.number %> - <%= company.name %></title>
  
  <!-- Inter Font -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  
  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            sans: ['Inter', 'system-ui', '-apple-system', 'sans-serif'],
          },
          colors: {
            brand: {
              50: '#f0fdfa',
              100: '#ccfbf1',
              500: '#14b8a6',
              600: '#0d9488',
              700: '#0f766e',
            }
          }
        }
      }
    }
  </script>
  
  <style>
    @media print {
      body {
        background: white !important;
        -webkit-print-color-adjust: exact;
        print-color-adjust: exact;
      }
      .invoice-card {
        box-shadow: none !important;
        max-width: 100% !important;
        margin: 0 !important;
        padding: 20px !important;
      }
      .no-print {
        display: none !important;
      }
      .page-break {
        page-break-before: always;
      }
    }
    
    /* Zebra striping for table rows */
    .zebra-stripe:nth-child(even) {
      background-color: #f9fafb;
    }
  </style>
</head>
<body class="bg-gray-50 min-h-screen font-sans antialiased">
  
  <!-- Print Button (hidden on print) -->
  <div class="no-print fixed top-4 right-4 z-50 flex gap-2">
    <button 
      onclick="window.print()" 
      class="bg-brand-600 hover:bg-brand-700 text-white px-4 py-2 rounded-lg shadow-lg flex items-center gap-2 transition-colors"
    >
      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 17h2a2 2 0 002-2v-4a2 2 0 00-2-2H5a2 2 0 00-2 2v4a2 2 0 002 2h2m2 4h6a2 2 0 002-2v-4a2 2 0 00-2-2H9a2 2 0 00-2 2v4a2 2 0 002 2zm8-12V5a2 2 0 00-2-2H9a2 2 0 00-2 2v4h10z" />
      </svg>
      Print Invoice
    </button>
    <button 
      onclick="window.close()" 
      class="bg-gray-600 hover:bg-gray-700 text-white px-4 py-2 rounded-lg shadow-lg flex items-center gap-2 transition-colors"
    >
      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
      </svg>
      Close
    </button>
  </div>

  <!-- Main Invoice Container -->
  <div class="py-8 px-4 sm:px-6 lg:px-8">
    <div class="invoice-card max-w-3xl mx-auto bg-white rounded-2xl shadow-xl overflow-hidden">
      
      <!-- Header Section -->
      <div class="bg-gradient-to-r from-brand-600 to-brand-500 px-8 py-6 text-white">
        <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-4">
          <!-- Company Info -->
          <div class="flex items-center gap-4">
            <% if (company.logoUrl) { %>
              <img src="<%= company.logoUrl %>" alt="<%= company.name %>" class="h-14 w-14 rounded-xl bg-white p-1 object-contain">
            <% } else { %>
              <div class="h-14 w-14 rounded-xl bg-white/20 flex items-center justify-center">
                <span class="text-2xl font-bold"><%= company.name.charAt(0) %></span>
              </div>
            <% } %>
            <div>
              <h1 class="text-xl sm:text-2xl font-bold"><%= company.name %></h1>
              <p class="text-brand-100 text-sm"><%= company.tagline %></p>
            </div>
          </div>
          
          <!-- Invoice Label -->
          <div class="text-left sm:text-right">
            <div class="inline-block bg-white/20 rounded-lg px-4 py-2">
              <p class="text-xs uppercase tracking-wider text-brand-100">Invoice</p>
              <p class="text-lg font-bold"><%= invoice.number %></p>
            </div>
          </div>
        </div>
      </div>

      <!-- Invoice Meta & Contact Row -->
      <div class="px-8 py-5 bg-gray-50 border-b border-gray-100">
        <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3 text-sm">
          <div class="flex items-center gap-4 text-gray-600">
            <div class="flex items-center gap-1.5">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
              </svg>
              <span><%= company.phone %></span>
            </div>
            <div class="flex items-center gap-1.5">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
              </svg>
              <span><%= company.email %></span>
            </div>
          </div>
          <div class="text-gray-600">
            <span class="font-medium text-gray-800">Date:</span> <%= invoice.date %>
          </div>
        </div>
      </div>

      <!-- Bill To & Payment Info Columns -->
      <div class="px-8 py-6">
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
          <!-- Bill To -->
          <div class="bg-gray-50 rounded-xl p-5 border border-gray-100">
            <div class="flex items-center gap-2 mb-3">
              <div class="w-1 h-5 bg-brand-500 rounded-full"></div>
              <h3 class="text-xs font-semibold uppercase tracking-wider text-gray-500">Bill To</h3>
            </div>
            <div class="space-y-2">
              <p class="text-base font-semibold text-gray-900"><%= client.name %></p>
              <p class="text-sm text-gray-600"><%= client.email %></p>
              <p class="text-sm text-gray-600 leading-relaxed"><%= client.address %></p>
              <% if (client.website) { %>
                <p class="text-sm text-brand-600"><%= client.website %></p>
              <% } %>
            </div>
          </div>

          <!-- Payment Info -->
          <div class="bg-gray-50 rounded-xl p-5 border border-gray-100">
            <div class="flex items-center gap-2 mb-3">
              <div class="w-1 h-5 bg-brand-500 rounded-full"></div>
              <h3 class="text-xs font-semibold uppercase tracking-wider text-gray-500">Payment Info</h3>
            </div>
            <div class="space-y-2 text-sm">
              <div class="flex justify-between">
                <span class="text-gray-500">Method:</span>
                <span class="font-medium text-gray-900"><%= payment.method %></span>
              </div>
              <div class="flex justify-between">
                <span class="text-gray-500">Beneficiary:</span>
                <span class="font-medium text-gray-900"><%= payment.beneficiaryName %></span>
              </div>
              <% if (payment.accountNumber) { %>
                <div class="flex justify-between">
                  <span class="text-gray-500">Account No:</span>
                  <span class="font-medium text-gray-900 font-mono"><%= payment.accountNumber %></span>
                </div>
              <% } %>
              <% if (payment.ifscCode) { %>
                <div class="flex justify-between">
                  <span class="text-gray-500">IFSC:</span>
                  <span class="font-medium text-gray-900 font-mono"><%= payment.ifscCode %></span>
                </div>
              <% } %>
              <% if (payment.bankName) { %>
                <div class="flex justify-between">
                  <span class="text-gray-500">Bank:</span>
                  <span class="font-medium text-gray-900"><%= payment.bankName %></span>
                </div>
              <% } %>
              <% if (payment.upiId) { %>
                <div class="flex justify-between border-t border-gray-200 pt-2 mt-2">
                  <span class="text-gray-500">UPI ID:</span>
                  <span class="font-medium text-brand-600"><%= payment.upiId %></span>
                </div>
              <% } %>
            </div>
          </div>
        </div>
      </div>

      <!-- Line Items Table -->
      <div class="px-8 pb-6">
        <div class="overflow-hidden rounded-xl border border-gray-200">
          <table class="w-full">
            <thead>
              <tr class="bg-brand-600 text-white">
                <th class="text-left px-4 py-3 text-xs font-semibold uppercase tracking-wider">Description</th>
                <th class="text-center px-4 py-3 text-xs font-semibold uppercase tracking-wider w-20">Qty</th>
                <th class="text-right px-4 py-3 text-xs font-semibold uppercase tracking-wider w-28">Unit Price</th>
                <th class="text-right px-4 py-3 text-xs font-semibold uppercase tracking-wider w-28">Amount</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-gray-100">
              <% items.forEach(function(item, index) { %>
                <tr class="zebra-stripe hover:bg-gray-50 transition-colors">
                  <td class="px-4 py-3.5 text-sm text-gray-900"><%= item.description %></td>
                  <td class="px-4 py-3.5 text-sm text-gray-600 text-center"><%= item.quantity %></td>
                  <td class="px-4 py-3.5 text-sm text-gray-600 text-right font-mono"><%= item.unitPrice %></td>
                  <td class="px-4 py-3.5 text-sm text-gray-900 text-right font-semibold font-mono"><%= item.amount %></td>
                </tr>
              <% }); %>
            </tbody>
          </table>
        </div>
      </div>

      <!-- Totals Section -->
      <div class="px-8 pb-6">
        <div class="flex justify-end">
          <div class="w-full sm:w-72">
            <div class="bg-gray-50 rounded-xl overflow-hidden border border-gray-200">
              <div class="divide-y divide-gray-200">
                <div class="flex justify-between px-4 py-3">
                  <span class="text-sm text-gray-500">Subtotal</span>
                  <span class="text-sm font-medium text-gray-900 font-mono"><%= totals.subtotal %></span>
                </div>
                <div class="flex justify-between px-4 py-3">
                  <span class="text-sm text-gray-500">Tax (<%= totals.taxRate || '0' %>%)</span>
                  <span class="text-sm font-medium text-gray-900 font-mono"><%= totals.tax %></span>
                </div>
                <div class="flex justify-between px-4 py-3 bg-gray-100">
                  <span class="text-sm font-medium text-gray-700">Grand Total</span>
                  <span class="text-sm font-bold text-gray-900 font-mono"><%= totals.grandTotal %></span>
                </div>
                <% if (totals.amountPaid && totals.amountPaidValue > 0) { %>
                  <div class="flex justify-between px-4 py-3">
                    <span class="text-sm text-green-600">Amount Paid</span>
                    <span class="text-sm font-medium text-green-600 font-mono">- <%= totals.amountPaid %></span>
                  </div>
                <% } %>
                <div class="flex justify-between px-4 py-4 bg-brand-600 text-white">
                  <span class="font-semibold">Balance Due</span>
                  <span class="text-lg font-bold font-mono"><%= totals.balanceDue %></span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Amount in Words (for balance due) -->
      <% if (totals.balanceInWords) { %>
        <div class="px-8 pb-6">
          <div class="bg-brand-50 rounded-lg px-4 py-3 border border-brand-100">
            <p class="text-sm text-brand-700">
              <span class="font-medium">Balance due in words:</span> <%= totals.balanceInWords %>
            </p>
          </div>
        </div>
      <% } %>

      <!-- Notes Section -->
      <% if (notes) { %>
        <div class="px-8 pb-6">
          <div class="bg-amber-50 rounded-xl p-5 border border-amber-100">
            <div class="flex items-start gap-3">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-amber-500 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              <div>
                <h4 class="text-sm font-semibold text-amber-800 mb-1">Notes</h4>
                <p class="text-sm text-amber-700 leading-relaxed"><%= notes %></p>
              </div>
            </div>
          </div>
        </div>
      <% } %>

      <!-- Terms Section -->
      <% if (terms) { %>
        <div class="px-8 pb-6">
          <div class="bg-gray-50 rounded-xl p-4 border border-gray-100">
            <h4 class="text-xs font-semibold uppercase tracking-wider text-gray-500 mb-2">Terms & Conditions</h4>
            <div class="text-xs text-gray-600 leading-relaxed space-y-1">
              <% 
                // Parse terms into lines - split by numbered patterns or newlines
                const termLines = terms.split(/(?=\d+[\.\)\:])|[\n\r]+/).filter(line => line.trim());
                termLines.forEach(function(line) { 
              %>
                <p class="mb-1"><%= line.trim() %></p>
              <% }); %>
            </div>
          </div>
        </div>
      <% } %>

      <!-- Footer: QR Code & Signature -->
      <div class="px-8 py-8 border-t border-gray-100 bg-gray-50">
        <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-8">
          <!-- QR Code Section -->
          <% if (footer.showQr || footer.upiId) { %>
            <div class="text-center sm:text-left flex-shrink-0">
              <p class="text-xs font-semibold uppercase tracking-wider text-gray-500 mb-3">Scan to Pay</p>
              <div class="flex flex-col sm:flex-row items-center sm:items-start gap-4">
                <% if (footer.qrCodeUrl) { %>
                  <div class="w-28 h-28 bg-white rounded-lg border border-gray-200 p-1 shadow-sm">
                    <img src="<%= footer.qrCodeUrl %>" alt="UPI QR Code" class="w-full h-full object-contain rounded">
                  </div>
                <% } else { %>
                  <div class="w-28 h-28 bg-white rounded-lg border-2 border-dashed border-gray-300 flex items-center justify-center">
                    <div class="text-center p-2">
                      <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10 text-gray-300 mx-auto" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v1m6 11h2m-6 0h-2v4m0-11v3m0 0h.01M12 12h4.01M16 20h4M4 12h4m12 0h.01M5 8h2a1 1 0 001-1V5a1 1 0 00-1-1H5a1 1 0 00-1 1v2a1 1 0 001 1zm12 0h2a1 1 0 001-1V5a1 1 0 00-1-1h-2a1 1 0 00-1 1v2a1 1 0 001 1zM5 20h2a1 1 0 001-1v-2a1 1 0 00-1-1H5a1 1 0 00-1 1v2a1 1 0 001 1z" />
                      </svg>
                      <p class="text-xs text-gray-400 mt-1">UPI QR</p>
                    </div>
                  </div>
                <% } %>
                <div class="text-center sm:text-left">
                  <% if (footer.upiId) { %>
                    <p class="text-sm text-brand-600 font-medium mb-1">UPI: <%= footer.upiId %></p>
                  <% } %>
                  <% if (footer.paymentLink) { %>
                    <p class="text-xs text-gray-500 mb-2">Or pay online:</p>
                    <a href="<%= footer.paymentLink %>" target="_blank" class="text-xs text-brand-600 hover:underline break-all">
                      <%= footer.paymentLink.length > 40 ? footer.paymentLink.substring(0, 40) + '...' : footer.paymentLink %>
                    </a>
                  <% } %>
                  <p class="text-xs text-gray-400 mt-2">Scan QR code with any UPI app</p>
                </div>
              </div>
            </div>
          <% } else { %>
            <div class="flex-1"></div>
          <% } %>

          <!-- Signature -->
          <div class="text-center sm:text-right flex-shrink-0">
            <div class="inline-block min-w-[180px]">
              <div class="w-48 border-b-2 border-gray-400 mb-3 h-14"></div>
              <p class="text-sm font-semibold text-gray-900"><%= footer.signatoryName %></p>
              <p class="text-xs text-gray-500 mt-1"><%= footer.signatoryTitle || 'Authorized Signatory' %></p>
              <p class="text-xs text-gray-400 mt-1"><%= footer.signDate %></p>
            </div>
          </div>
        </div>
      </div>

      <!-- Footer Branding -->
      <div class="px-8 py-4 bg-gray-900 text-center">
        <p class="text-xs text-gray-400">
          Thank you for your business! | Generated by <span class="text-brand-400 font-medium"><%= company.name %></span>
        </p>
      </div>

    </div>
  </div>

</body>
</html>


